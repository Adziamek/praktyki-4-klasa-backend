{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },

  "AllowedHosts": "*"

  // TODO: Configure variables and better to trohw this to user secrets

  //"ConnectionStrings": {
  //  "WarehouseDb": "Host=localhost;Port=5432;Database=wms;Username=postgres;Password=YOUR-PASSWORD"
  //}

  //"Admin:Username": "YOUR-USERNAME", 
  //"Admin:Password": "YOUR-PASSWORD"

  //"Jwt:Key": "YOUR-JWT-KEY",    <------ You can generate key by using node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
  //"Jwt:Issuer": "JWT-ISSUER",   <------ For example: wms
  //"Jwt:Audience": "JWT-AUDIENCE" <------ For example: wms
}

